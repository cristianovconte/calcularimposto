SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE P_NOTA_FISCAL_IMPOSTOS

AS
BEGIN
	
	SET NOCOUNT ON;

	SELECT Cfop, SUM(BaseIcms), SUM(ValorIcms), SUM(BaseIPI), SUM(ValorIPI) from NotaFiscalItem
	GROUP BY Cfop

END
GO
